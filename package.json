{
  "name": "my-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.0.0"
  }
}
